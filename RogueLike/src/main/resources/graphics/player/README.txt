Each folder here, contains all the images for an animation.
The position of each file in these folders are important, because that'll be the order in which the animation is played.

Each folder also contains a config.txt file, telling how long (in seconds) the animation is supposed to be.